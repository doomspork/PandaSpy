{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the main window. Kept deliberately small: Spool handles printer access codes, so every capability added here needs a reason.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
