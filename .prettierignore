# Generated
.svelte-kit/
build/
target/
node_modules/
pnpm-lock.yaml

# Owned by rustfmt, not prettier.
src-tauri/
crates/
xtask/

# Owned by Release Please, which rewrites them in its own style on every
# release PR. If prettier also has an opinion, that PR fails CI for a reason
# nobody can act on — Release Please will just reformat it again next time.
CHANGELOG.md
.release-please-manifest.json
release-please-config.json

# Fluent is not a format prettier understands; reflowing it would corrupt
# multiline patterns.
locales/

# Recordings. A fixture is a byte-for-byte capture; reformatting one changes
# what it is testing.
fixtures/
